{
  "buildCommand": "npm install --prefix client && cd client && npm run build && npm install --prefix ../server && cd ../server && npm run build",
  "outputDirectory": "client/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    }
  ]
}